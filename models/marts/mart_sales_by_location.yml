models:
  - name: mart_sales_by_location
    description: One row per store location, with revenue rolled up. Closest real geographic dimension this dataset has (no country/region on the order itself).
    columns:
      - name: location_id
        data_tests:
          - unique
