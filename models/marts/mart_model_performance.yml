models:
  - name: mart_model_performance
    description: >
      One row per model execution (each time a jaffle_shop-package model
      was built within any invocation), with duration and status, for
      build-time trend and slow-model analysis.
    columns:
      - name: model_execution_id
        data_tests:
          - not_null
          - unique
