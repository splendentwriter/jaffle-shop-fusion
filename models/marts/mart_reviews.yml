models:
  - name: mart_reviews
    description: >
      One row per review, with product name attached and an is_negative
      flag (rating <= 2) for moderation and response-rate tracking.
    columns:
      - name: review_id
        data_tests:
          - not_null
          - unique
