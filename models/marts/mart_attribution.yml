models:
  - name: mart_attribution
    description: >
      One row per last-touch attributed checkout, with channel, time-to-
      convert, and attributed revenue — the row-level detail behind
      mart_campaign_performance's per-campaign rollup.
    columns:
      - name: checkout_id
        data_tests:
          - not_null
          - unique
