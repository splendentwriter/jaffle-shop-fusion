models:
  - name: mart_shipping_delivery
    description: >
      One row per shipment with carrier attached, delivery timing, and the
      was_late / is_delivered flags for on-time delivery analysis.
    data_tests:
      - elementary.schema_changes
    columns:
      - name: shipment_id
        data_tests:
          - not_null
          - unique
