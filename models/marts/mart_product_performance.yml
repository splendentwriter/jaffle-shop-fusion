models:
  - name: mart_product_performance
    description: >
      One row per product that has at least one order line, with revenue,
      units, margin (using order_items.supply_cost as COGS), and rating
      rolled up.
    columns:
      - name: product_id
        data_tests:
          - not_null
          - unique
