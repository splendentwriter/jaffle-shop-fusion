models:
  - name: mart_pipeline_health
    description: >
      One row per dbt invocation (each `dbt build` run, whether local or
      CI), rolling up model/test success and failure counts and total
      execution time for a pipeline run-history view.
    columns:
      - name: invocation_id
        data_tests:
          - not_null
          - unique
