models:
  - name: dim_brand
    description: One row per brand.
    data_tests:
      - elementary.schema_changes
    columns:
      - name: brand_id
        data_tests:
          - not_null
          - unique
