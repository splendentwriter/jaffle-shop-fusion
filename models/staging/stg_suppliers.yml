models:
  - name: stg_suppliers
    description: One row per supplier/vendor of finished products.
    data_tests:
      - elementary.schema_changes
    columns:
      - name: supplier_id
        data_tests:
          - not_null
          - unique
